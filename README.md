# Whatsapp-call-ip-grabber
This script is a real-time WhatsApp STUN traffic analyzer that identifies the receiving end’s public IP during a call, performs geolocation, logs results to an HTML file with embedded maps, detects call start/stop
